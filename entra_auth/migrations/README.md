"""
entra_auth has no models of its own.

It relies entirely on Django's built-in auth models (User, Group) so no
migrations are needed.  This file exists only to satisfy Django's app
migration detection.
"""
